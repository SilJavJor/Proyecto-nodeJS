# Front-end

## Instalación

- Ir a la carpeta `front` y ejecutar:

   ```bash
   npm install

- Esto instalara las dependencias que se encuentran en el archivo pakage.json.

## Ejecución

- Abrir la carpeta front en Visual Studio Code.

- Usar la extensión Live Server o Http Server para ejecutar la página en modo local.
